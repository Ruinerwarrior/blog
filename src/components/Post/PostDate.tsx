import styled from "styled-components";

const PostDate = styled.p`
  grid-column: 2;
  grid-row: 1;
  align-self: end;
  justify-self: left;
  margin-bottom: 0;
  color: grey;
`;

export default PostDate;
